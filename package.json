{
    "name": "ultraskor.com",
    "version": "1.0.0",
    "description": "ultraskor.com - A live score app built using ReactJS, NodeJs, Express and MongoDB that provides live coverage for almost all football leagues",
    "author": "Mustafa Cakir",
    "license": "MIT",
    "engines": {
        "node": ">=8.0.0"
    },
    "dependencies": {
        "concurrently": "*"
    },
    "devDependencies": {
        "eslint": "^6.5.0",
        "eslint-config-airbnb": "^18.0.1",
        "eslint-config-prettier": "^6.0.0",
        "eslint-plugin-import": "^2.18.2",
        "eslint-plugin-jsx-a11y": "^6.2.3",
        "eslint-plugin-prettier": "^3.1.0",
        "eslint-plugin-react": "^7.14.3",
        "node-sass": "^4.12.0",
        "prettier": "^1.18.2"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "scripts": {
        "dev": "concurrently \"cd server && yarn start-dev\" \"cd client && yarn start\"",
        "prod": "concurrently \"cd server && yarn install && yarn start-prod\" \"cd client && yarn install && yarn build\""
    }
}
